spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/pangzi?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: root
  thymeleaf:
    prefix:
      classpath: /templates/
    cache: false
  mybatis:
    mapper-locations: classpath:Mapper/*.xml
    configuration:
      map-underscore-to-camel-case: true
  jpa:
    database: oracle
    show-sql: true
  mvc:
    static-path-pattern: /static/**
mybatis:
  mapper-locations: classpath:Mapper/*.xml
server:
  port: 8081

